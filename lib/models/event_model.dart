class EventModel {
  String name;
  // num datetime;
  // For now a hard-coded date num and time string, will probably have to be a timestamp later
  num date;
  String time;

  EventModel(this.name, this.date, this.time);
}
